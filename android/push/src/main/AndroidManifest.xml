<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <meta-data
            android:name="com.jiangker.push.server"
            android:value="${PUSH_HUB_SERVER}" />
        <meta-data
            android:name="com.jiangker.push.app_id"
            android:value="${PUSH_HUB_APP_ID}" />
    </application>

</manifest>
